---
interface Props {
  /** Main heading inside the hero banner. */
  title: string;
}

const { title } = Astro.props;
---
<section id="banner">
  <div class="inner">
    <div class="logo"><span class="icon fa-gem"></span></div>
    <h2>{title}</h2>
    <slot />
  </div>
</section>
